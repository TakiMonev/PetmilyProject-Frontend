import { View, StyleSheet } from 'react-native';

const ListHealthScreen = () => {
  return <View>건강리스트</View>;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 20,
  },
});

export default ListHealthScreen;
